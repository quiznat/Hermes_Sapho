# Legacy Compatibility Pack

This directory is a retired compatibility mirror.

Canonical Synthesist packs now live under:

- `synthesist/article`
- `synthesist/daily`

This directory remains only so the current harness can keep reading the existing flat layout until the loader is updated.
