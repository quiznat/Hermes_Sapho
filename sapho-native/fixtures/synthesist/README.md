# Synthesist

`Synthesist` is one persona with two Daily jobs:

- `article`
- `daily`

The canonical fixture packs live in the matching subdirectories.
The old `synthesist-article` and `synthesist-daily` roots are compatibility aliases only.
